"""reconagent — an agent that triages a ledger reconciliation exception queue.

Project B: a real tool surface (11 Pydantic-typed tools), two agent loops over
that surface (hand-rolled and framework-driven), the same tools re-exported as
an MCP server, and an eval harness that scores task success, steps and cost.
"""

__version__ = "0.1.0"
